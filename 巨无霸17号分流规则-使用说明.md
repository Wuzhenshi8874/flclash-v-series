# 巨无霸17号分流规则 使用说明

> 适用对象：FlClash / FlyClash / Bettbox / 标准 Mihomo 内核用户。  
> 当前定位：在巨无霸16号基础上同步 V61 骨架维护能力，继续保留巨无霸系列的大体积内联规则与历史专项修复。

---

## 1. 这是什么？

**巨无霸17号** 是一份 FlClash 覆写脚本，用来把机场订阅生成的 Mihomo 配置重新整理成更适合日常使用的分流结构。

它不是机场订阅，也不包含节点、账号、密码或密钥。它的作用是：

- 自动整理节点分组；
- 按业务分流 App 和域名；
- 修复常见误拦截；
- 优化 DNS、Fake-IP、嗅探与系统服务兼容；
- 保留广告/威胁拦截，但尽量避免误伤登录、金融、系统更新和应用商店。

一句话：

```text
机场订阅负责提供节点
巨无霸17号负责把流量分对组、走对出口、减少误杀
```

---

## 2. 适合谁用？

推荐给这些用户：

- 使用 FlClash 的 Android 用户；
- 使用 FlyClash / Bettbox 等 Mihomo 系客户端的用户；
- 有多个节点、多个地区、多个业务分组需求的人；
- 经常遇到 Google Play、TikTok、OKX、giffgaff、微软/Xbox、小米系统更新、迅雷、YouTube 字幕等问题的人；
- 希望广告拦截继续保留，但不想因为规则太激进导致 App 登录失败的人。

不推荐给这些情况：

- 只想最简单开关代理；
- 不会看日志，也不愿意按步骤排查；
- 机场订阅本身已经坏了或节点大量不可用；
- 客户端不支持 JavaScript 覆写脚本。

---

## 3. 巨无霸17号分流规则主要修了什么？

巨无霸17号保留了巨无霸16号的完整主干，并同步 V61 骨架能力。

重点包括：

| 模块 | 说明 |
|---|---|
| V61 骨架 | 快速失败、无效 provider 清理、规则维护能力 |
| 自动优选 | 坏节点更快踢出，减少长时间占用失效节点 |
| 广告拦截 | 功能规则优先，广告/威胁规则靠后兜底 |
| 小米 OTA | HyperOS / MIUI 更新域名和下载辅助链路优先 DIRECT |
| OKX / 欧易 | 官方域名、备用域名、Web3、行情链路统一进入加密货币组 |
| giffgaff | 登录页 Dynamic Yield / OneTrust 兼容，减少登录页卡死 |
| TikTok | 独立 TikTok 分组、修改版依赖和局域网代理兼容 |
| 微软 / Xbox | Store、Xbox、Gaming Services、UWP Fake-IP 兼容 |
| 迅雷 | 官网、账号、会员、云盘、下载/CDN 直连修复 |
| 中国访问 | 国内 DNS、`.cn`、腾讯/小米/国内 App 兼容增强 |
| ASN | 补齐 GeoLite2 ASN 数据库地址，避免 ASN 数据库类型错误 |

---

## 4. 文件应该怎么放？

建议文件名保持：

```text
巨无霸17号.js
```

如果你拿到的是：

```text
巨无霸17号.js.txt
```

这通常是为了手机方便保存和复制。导入 FlClash 时可以直接复制里面的全部内容；如果你要放到 GitHub 仓库，则建议把文件名改成：

```text
巨无霸17号.js
```

压缩包建议命名为：

```text
巨无霸17号.js.zip
```

---

## 5. FlClash 导入方法

### 第一步：创建覆写脚本

打开 FlClash：

```text
FlClash → 配置 → 覆写脚本 → 右上角 +
```

名称建议写：

```text
巨无霸17号
```

然后选择一种方式导入。

### 方式 A：手动粘贴，最稳

1. 打开 `巨无霸17号.js.txt`；
2. 全选；
3. 复制全部内容；
4. 回到 FlClash 覆写脚本编辑页面；
5. 粘贴；
6. 保存。

这是手机上最稳的方式，适合 GitHub Raw 打不开或 CDN 缓存没刷新的情况。

### 方式 B：URL 导入

如果你已经把脚本文件上传到 GitHub 根目录，并命名为 `巨无霸17号.js`，可以使用 Raw 地址：

```text
https://raw.githubusercontent.com/Wuzhenshi8874/flclash-v-series/main/%E5%B7%A8%E6%97%A0%E9%9C%B817%E5%8F%B7.js
```

如果 Raw 被墙或打不开，可以尝试 jsDelivr：

```text
https://cdn.jsdelivr.net/gh/Wuzhenshi8874/flclash-v-series@main/%E5%B7%A8%E6%97%A0%E9%9C%B817%E5%8F%B7.js
```

注意：jsDelivr 可能有缓存，刚上传后不一定马上刷新。

---

## 6. 关联订阅

只创建脚本还不够，必须把脚本绑定到机场订阅。

操作路径：

```text
配置页 → 找到你的订阅卡片 → 右上角 ⋮ → 更多 → 覆写
→ 选择“巨无霸17号” → 确定 → 下拉刷新订阅
```

如果忘了这一步，脚本不会生效。

---

## 7. 导入后必做检查

导入后先不要急着说好不好用，先看这几个地方。

### 7.1 看策略组有没有生成

代理页面应该能看到类似这些组：

```text
🎯 节点选择
🚀 自动优选
⚡ 极速自动
🛟 自动兜底
🤖 AI 服务
₿ 加密货币
💳 金融支付
🎵 TikTok
🛒 Google Play
Ⓜ️ 微软服务
🛡️ 广告拦截
🐟 漏网之鱼
```

如果一个都没有，说明脚本没有关联订阅，或者脚本导入失败。

### 7.2 看日志有没有低级错误

日志里不应该出现：

```text
SyntaxError
proxy not found
rule provider not found
Unsupported rule type
Unsupported ASN type
Cannot read property
```

如果出现这些，先不要继续乱改，把日志保存下来再排查。

### 7.3 看节点选择

建议先设置：

```text
🎯 节点选择 → 🚀 自动优选
```

如果你要登录金融、giffgaff、OKX、Microsoft、Google 账号，建议相关分组临时固定同一个稳定节点，避免登录过程中出口 IP 来回变。

---

## 8. 推荐分组设置

### 日常通用

```text
🎯 节点选择 → 🚀 自动优选
🤖 AI 服务 → 🎯 节点选择
🛒 Google Play → 🎯 节点选择
₿ 加密货币 → 🎯 节点选择
💳 金融支付 → 🎯 节点选择
🌍 国外网站 → 🎯 节点选择
🐟 漏网之鱼 → 🎯 节点选择
🛡️ 广告拦截 → REJECT
```

### 登录/注册/风控场景

登录银行、交易所、giffgaff、微软、Google、OpenAI 等账号时，建议临时固定：

```text
🤖 AI 服务
🛒 Google Play
💳 金融支付
₿ 加密货币
Ⓜ️ 微软服务
🌍 国外网站
```

到同一个稳定节点。

不要登录到一半从香港跳日本、从日本跳美国，否则会话可能失效。

### TikTok

TikTok 建议：

```text
🎵 TikTok → 固定日本 / 美国 / 新加坡稳定节点
```

不要一边刷视频一边频繁切节点。局域网 HTTP 代理无法完整接管 UDP/QUIC，视频一直转圈时优先考虑 TUN/VPN 接管，而不是继续乱加域名。

### 小米系统更新

系统更新建议：

```text
小米 OTA 主域名 → DIRECT
迅雷 OTA 辅助链路 → DIRECT
```

巨无霸17号已经保留巨无霸16号的小米 OTA 修复。更新失败时先看日志里是否还有：

```text
dns resolve failed: couldn't find ip
ultimateota.d.miui.com
superota.d.miui.com
```

---

## 9. 常见问题

### 9.1 保存脚本后没有任何变化

原因通常是：只创建脚本，没有关联订阅。

重新执行：

```text
订阅卡片 → 更多 → 覆写 → 选择巨无霸17号 → 刷新
```

### 9.2 提示 unexpected token `<`

你复制到了 GitHub 网页 HTML，而不是 JS 原文。

解决方法：

- 打开 Raw；
- 或者直接复制 `.js.txt` 文件里的纯文本；
- 不要复制 GitHub 普通网页页面。

### 9.3 Google Play 下载不了

先看日志是否命中：

```text
🛒 Google Play
```

如果系统下载器 `com.android.providers.downloads` 被强行 DIRECT，可能会导致 Google Play 下载失败。巨无霸17号保留系统下载器进入 Google Play，只把小米 OTA 域名提前 DIRECT。

### 9.4 OKX / 欧易部分接口跑漏网之鱼

巨无霸17号已经同步 OKX 全流量归组补丁。正常应进入：

```text
₿ 加密货币
```

如果还有 `ouyich.biz / ouyich.show / cnouyi.pizza` 跑到漏网之鱼，把日志发出来继续补。

### 9.5 giffgaff 登录失败

巨无霸17号已经放行登录页关键链路：

```text
cdn-eu.dynamicyield.com
privacyportal-uk.onetrust.com
mobile-data.onetrust.io
```

登录时建议把 `🌍 国外网站` 固定到英国、新加坡、日本或美国稳定节点，不要自动乱跳。

### 9.6 Microsoft Store / Xbox 还是转圈

先确认：

- TUN/VPN 是否真的开启；
- 系统代理模式是否不完整；
- 微软相关流量是否进入 `Ⓜ️ 微软服务`；
- 是否还有 DNS 泄漏或 Fake-IP 兼容问题。

巨无霸17号同步了 V59/V60 的微软 UWP / Xbox 真实 IP 兼容，但 Windows 和 Android 的网络环境仍要看实际日志。

### 9.7 广告拦截该选什么？

推荐：

```text
🛡️ 广告拦截 → REJECT
```

如果某个 App 登录失败，可以临时改成：

```text
🛡️ 广告拦截 → DIRECT
```

用来判断是不是误杀。确认误杀后，应该精准加白名单，而不是长期关闭广告拦截。

---

## 10. 出问题时怎么反馈日志

反馈时尽量提供：

1. 出问题的 App 名称；
2. App 包名；
3. 出问题的时间点；
4. 当时使用的策略组和节点；
5. FlClash 日志；
6. 是否使用局域网代理、系统代理、TUN、VPN；
7. 是否中途切换节点；
8. 错误截图。

最好不要只说“打不开”。要说明：

```text
哪个 App
点了哪里
卡在哪一步
大概几点几分
日志里有什么域名或错误
```

这样才能精准修，不会乱加规则。

---

## 11. 安全注意事项

- 不要公开上传带订阅 token 的日志；
- 不要把机场订阅链接写进 GitHub；
- 不要把银行卡、邮箱、手机号、Cookie、API Key 写进 issue；
- 不要清除 OKX、钱包、银行 App 数据，除非确认助记词/私钥/备份已安全保存；
- 不要为了修一个 App 就全局关闭广告/威胁拦截；
- 不确定的规则先问，再改。

---

## 12. 回滚方法

如果巨无霸17号导入后出现大问题：

1. 回到配置页；
2. 找到订阅卡片；
3. 进入更多 → 覆写；
4. 取消巨无霸17号，或切回上一版；
5. 下拉刷新订阅；
6. 重启 FlClash。

建议每次升级前都保存上一版脚本文件。

---

## 13. 维护原则

巨无霸系列维护原则：

```text
宁可少动，也不要误动
只修日志确认的问题
不乱删旧规则
不乱改分组结构
不粗暴放行大域名
不为一个 App 牺牲整套安全规则
```

下一次继续迭代时，建议版本号递增为：

```text
巨无霸18号
```

并保留详细更新日志。
